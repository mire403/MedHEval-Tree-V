# Kvasir-VQA-x1 Data

This folder is reserved for local dataset artifacts if you prefer to keep data under `data/`.

The experiment scripts currently expect the parquet files at:

```text
step3_dataset_migration/raw/kvasir_vqa_x1/
```

Raw dataset files are intentionally excluded from the GitHub release.

