# SLAKE Placeholder

Place the SLAKE dataset files here for the external stress test.

Use the inspection command first:

```bash
python step5_experiments/server_scripts/slake_external_validation.py inspect --data-dir data/slake
```

Raw SLAKE images and annotations are not redistributed in this repository.

